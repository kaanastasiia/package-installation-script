Script does the following:
1) It checks if the specified package is installed
2) If the packages is not installed, it will be installed via the script

Note: the script works only for debian-based distributions. 

Example: install <name-of-the-package>
